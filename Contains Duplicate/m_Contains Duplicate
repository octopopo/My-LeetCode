class Solution {
public:
    bool containsDuplicate(vector<int>& nums) {
        unordered_set<int> m_set;
        int nL = nums.size();
        for(int i = 0; i < nL; i++)
        {
            if(m_set.find(nums[i]) != m_set.end())
            {
                return true;
            }
            else
            {
                m_set.insert(nums[i]);
            }
        }
        
        return false;
    }
};